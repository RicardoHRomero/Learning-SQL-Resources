# Sorting Data

/* 
This statement is identical to the earlier statement, except it also specifies
an ORDER BY clause instructing the DBMS software to sort the data by the prod_name column. 
*/

SELECT prod_name 
FROM Products
ORDER BY prod_name;